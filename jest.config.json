{
  "preset": "jest-preset-angular",
  "setupFilesAfterEnv": ["<rootDir>/setup-jest.ts"],
  "testTimeout": 5000,
  "bail": true,
  "verbose": true,
  "testMatch": [
    "<rootDir>/test/**/*.spec.ts?(x)"
  ]
}
